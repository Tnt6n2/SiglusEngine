
■オブジェクトに３Ｄオブジェクトを読み込み、表示する

	front.object[0].create_mesh($file_name, $disp, $x, $y)

	アドベンチャーの演出として３Ｄオブジェクトを表示する。

	・独自形式の３Ｄファイルからメッシュを読み込む
		→ x ファイルのバイナリ形式を独自暗号化
		→ lwo（Light Wave Object）などからコンバートできれば素敵！
	・ビルボード
	・アルファチャンネル
	・テクスチャの張替えなどは？その他のパラメータの変更は？
	・スキンメッシュ、アニメーションにも対応
	・当たり判定は？

■ワールドの概念

	・１つのワールドは１つのカメラを持つ
	・同じ３Ｄ空間に存在するオブジェクトは同じワールドに属する
	・同じワールドに存在するオブジェクトはＺソートを行う
	・アルファチャンネルつきメッシュの場合のＺソートは？

■カメラの設定

	・左手座標系
		２ＤとＹ座標の向きが違うが問題ない？

	・カメラのパラメータの指定方法は？
		・位置、注視点位置（座標）、上方向の３ベクトル
		・位置、注視点位置（距離と角度）、上方向の３ベクトル
		・注視点位置（座標）、位置（距離と角度）、上方向の３ベクトル

	・座標の簡易設定
		・イベントを使う？
		・注視点を固定し、カメラのアングルを簡単にまわす機能など
		・ジンバルロックは？クォータニオンとか必要？

	・視野角の設定

	・Siglus では少数は扱えない。どうするべきか。
		・ワールドごとに単位を設定できるようにする（1000=1.0 など）
		・デフォルト値をどうするか？

	front.object[0].world = 0
	front.object[0].create_mesh(cat00, 1, 0, 0)
	world[0].unit = 1000
	world[0].set_camera_pos(0, 0, 1000)
	world[0].set_pint_pos(0, 0, 0)
	world[0].set_up_pos(0, 0, 0)

■ライトの設定

■高度な設定

	・ステンシルバッファ？
	・その他

■やるべきこと

	・仕様制定
	・オブジェクトの拡張
	・コマンドの追加
	・マニュアルの更新
	・ツールの作成


